这是一份为Meshtastic开发APRS功能模块的需求文档，重点涵盖了独立APRS Frame的实现以及与系统UI和按键响应的集成。

Meshtastic APRS功能模块开发需求文档

1. 文档概述

本文档旨在定义在Meshtastic设备上开发APRS（自动封包报告系统）功能模块的技术需求。核心目标是实现一个与现有系统UI风格一致、交互逻辑统一的APRS功能界面，并支持通过长按按钮触发功能菜单。该模块将扩展Meshtastic设备在业余无线电和位置追踪场景下的应用能力。

2. 功能需求

2.1 核心功能
APRS Frame：实现一个独立的APRS用户界面（Frame），用于显示APRS相关状态和信息。
统一外观：该Frame的视觉风格、布局逻辑必须与Meshtastic系统的其他Frame（如消息、地图、设置等）保持高度一致。
按键响应：Frame必须完全遵循Meshtastic系统的全局按键响应规范，确保用户操作体验的一致性。

2.2 动作菜单（Action Menu）

触发方式：通过长按设备上的指定按键呼出动作菜单。
菜单选项：菜单至少应包含两项功能：

手动发射（Manual Beacon）：立即发送一次包含当前GPS位置信息的APRS信标。
退出（Exit）：关闭APRS Frame并返回上级界面。

状态机逻辑：按键检测需采用状态机模型，以可靠地区分短按、长按等不同操作意图，并进行消抖处理。

3. 技术规格
3.1 APRS Frame设计
该Frame是APRS功能的主界面，其设计需遵循以下原则：

组件要求说明标题栏显示“APRS”字样及信号强度指示与系统风格一致。
内容区显示本机呼号、最后发射时间、接收到的APRS站台列表等。布局清晰，信息密度适中。状态栏显示连接状态、GPS锁定状态等，使用系统标准状态图标。

3.2 事件系统集成
模块必须与Meshtastic的事件驱动架构深度集成：
注册观察者：APRS模块应注册为相关事件（如MeshPacket事件、InputEvent事件）的观察者。
事件处理：
当Meshtastic发送消息或定位信息时，在每次发送后，切换到APRS的频率用lora aprs相关参数发送APRS数据包（包含当前GPS位置信息），发送完毕恢复到meshtastic设定频率。

当产生用户输入事件时，调用统一的handleInputEvent方法处理按键响应。

3.3 数据编码与发送

位置获取：通过Meshtastic的PositionService获取当前经纬度、海拔等信息。
数据封装：将位置等信息封装成标准的APRS协议数据帧。
报文发送：利用Meshtastic的MeshService接口，将封装好的APRS数据作为MeshPacket的负载（Payload）发送至LoRa网状网络。

4. 非功能性需求

性能：界面响应应流畅，按键响应延迟应低于100毫秒。
功耗：在非主动发射状态下，模块应处于低功耗模式，尽量减少对设备续航的影响。
内存管理：使用Meshtastic系统的内存池机制分配数据包，避免内存碎片。

5. 后续可扩展性考虑

功能扩展：为动作菜单预留接口，便于未来增加“自动发射间隔设置”、“查看历史信标”等功能。
协议互通：考虑未来与互联网APRS-IS网关集成的可能性，实现更广范围的APRS通信。